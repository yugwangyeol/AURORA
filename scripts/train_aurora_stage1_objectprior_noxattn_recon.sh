#!/bin/bash
# =============================================================================
# AURORA Stage 1 wrapper: RefCOCO-family multi-slot recon baseline
# =============================================================================

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export MODEL_PATH="${MODEL_PATH:-/home/jovyan/data/Scale-RAE-Qwen1.5B_DiT2.4B}"
export OUTPUT_DIR="${OUTPUT_DIR:-/home/jovyan/AURORA/checkpoints/aurora_refcoco_multislot_recon_stage1}"
export EXPERIMENT_NAME="${EXPERIMENT_NAME:-aurora_refcoco_multislot_recon_stage1}"
export RESUME_FROM_CHECKPOINT="${RESUME_FROM_CHECKPOINT:-}"

export NUM_GPUS="${NUM_GPUS:-2}"
export MAX_STEPS="${MAX_STEPS:-40000}"
export NUM_TRAIN_EPOCHS="${NUM_TRAIN_EPOCHS:-1}"
export PER_DEVICE_TRAIN_BATCH_SIZE="${PER_DEVICE_TRAIN_BATCH_SIZE:-16}"
export PER_DEVICE_EVAL_BATCH_SIZE="${PER_DEVICE_EVAL_BATCH_SIZE:-4}"
export GRADIENT_ACCUMULATION_STEPS="${GRADIENT_ACCUMULATION_STEPS:-1}"
export LOGGING_STEPS="${LOGGING_STEPS:-10}"
export EVAL_STEPS="${EVAL_STEPS:-1000}"
export SAVE_STEPS="${SAVE_STEPS:-1000}"
export DATALOADER_NUM_WORKERS="${DATALOADER_NUM_WORKERS:-8}"
export REPORT_TO="${REPORT_TO:-wandb}"
export BF16="${BF16:-True}"
export TF32="${TF32:-True}"
export LEARNING_RATE="${LEARNING_RATE:-5e-5}"
export DIFF_HEAD_LR="${DIFF_HEAD_LR:-3e-5}"
export WARMUP_RATIO="${WARMUP_RATIO:-0.05}"
export LR_SCHEDULER_TYPE="${LR_SCHEDULER_TYPE:-cosine}"
export CAPTIONSLOT_LATENT_QUERY_LR="${CAPTIONSLOT_LATENT_QUERY_LR:-}"
export GRADIENT_CHECKPOINTING="${GRADIENT_CHECKPOINTING:-False}"

export VISION_TOWER_AUX_LIST="${VISION_TOWER_AUX_LIST:-[\"google/siglip2-so400m-patch16-512\",\"google/siglip2-so400m-patch14-224\"]}"
export VISION_TOWER_AUX_TOKEN_LEN_LIST="${VISION_TOWER_AUX_TOKEN_LEN_LIST:-[1024,256]}"
export IMAGE_FEATURE_TOKEN_LEN="${IMAGE_FEATURE_TOKEN_LEN:-1024}"
export DIFFUSION_TARGET_TOKEN_LEN="${DIFFUSION_TARGET_TOKEN_LEN:-256}"

export CAPTIONSLOT_TRAINING_STAGE=1
export CAPTIONSLOT_CONTROL_MODE="${CAPTIONSLOT_CONTROL_MODE:-slots}"
export CAPTIONSLOT_MAX_SLOTS="${CAPTIONSLOT_MAX_SLOTS:-48}"
export CAPTIONSLOT_SLOTS_PER_OBJECT="${CAPTIONSLOT_SLOTS_PER_OBJECT:-4}"
export CAPTIONSLOT_N_REGISTER="${CAPTIONSLOT_N_REGISTER:-64}"
export CAPTIONSLOT_CMD_LENGTH="${CAPTIONSLOT_CMD_LENGTH:-8}"
export CAPTIONSLOT_RECON_LOSS_WEIGHT="${CAPTIONSLOT_RECON_LOSS_WEIGHT:-1.0}"
export CAPTIONSLOT_MASK_BCE_LOSS_WEIGHT="${CAPTIONSLOT_MASK_BCE_LOSS_WEIGHT:-1.0}"
export CAPTIONSLOT_MASK_DICE_LOSS_WEIGHT="${CAPTIONSLOT_MASK_DICE_LOSS_WEIGHT:-1.0}"
export CAPTIONSLOT_CAPTION_LOSS_WEIGHT="${CAPTIONSLOT_CAPTION_LOSS_WEIGHT:-0.0}"
export CAPTIONSLOT_DIVERSITY_LOSS_WEIGHT="${CAPTIONSLOT_DIVERSITY_LOSS_WEIGHT:-0.0}"
export CAPTIONSLOT_TRAIN_LATENT_QUERIES="${CAPTIONSLOT_TRAIN_LATENT_QUERIES:-False}"
export CAPTIONSLOT_ATTENTION_USE_LAYERNORM="${CAPTIONSLOT_ATTENTION_USE_LAYERNORM:-True}"
export CAPTIONSLOT_ATTENTION_TEMPERATURE="${CAPTIONSLOT_ATTENTION_TEMPERATURE:-1.0}"
export CAPTIONSLOT_PRIOR_BIAS_SCALE="${CAPTIONSLOT_PRIOR_BIAS_SCALE:-0.0}"
export CAPTIONSLOT_ADD_CROSS_ATTENTION="${CAPTIONSLOT_ADD_CROSS_ATTENTION:-False}"
export CAPTIONSLOT_CROSS_ATTENTION_START_BLOCK="${CAPTIONSLOT_CROSS_ATTENTION_START_BLOCK:-8}"
export CAPTIONSLOT_CROSS_ATTENTION_EVERY_N_BLOCKS="${CAPTIONSLOT_CROSS_ATTENTION_EVERY_N_BLOCKS:-4}"
export CAPTIONSLOT_CROSS_ATTENTION_INCLUDE_REGISTERS="${CAPTIONSLOT_CROSS_ATTENTION_INCLUDE_REGISTERS:-True}"
export CAPTIONSLOT_USE_WSD_SCHEDULE="${CAPTIONSLOT_USE_WSD_SCHEDULE:-False}"
export CAPTIONSLOT_WSD_DECAY_FRACTION="${CAPTIONSLOT_WSD_DECAY_FRACTION:-0.10}"
export CAPTIONSLOT_USE_COSINE_MIN_LR_SCHEDULE="${CAPTIONSLOT_USE_COSINE_MIN_LR_SCHEDULE:-True}"
export CAPTIONSLOT_MIN_LR_RATIO="${CAPTIONSLOT_MIN_LR_RATIO:-0.10}"
export CAPTIONSLOT_EVAL_NUM_IMAGES="${CAPTIONSLOT_EVAL_NUM_IMAGES:-64}"

export CAPTIONSLOT_COCO_ROOT="${CAPTIONSLOT_COCO_ROOT:-/home/jovyan/data/coco}"
export CAPTIONSLOT_DATASETS="${CAPTIONSLOT_DATASETS:-refcoco,refcoco+,refcocog}"
export CAPTIONSLOT_TRAIN_SPLITS="${CAPTIONSLOT_TRAIN_SPLITS:-train}"
export CAPTIONSLOT_EVAL_SPLITS="${CAPTIONSLOT_EVAL_SPLITS:-val}"
export CAPTIONSLOT_MIN_AREA="${CAPTIONSLOT_MIN_AREA:-0}"
export MAX_CAPTION_TOKENS="${MAX_CAPTION_TOKENS:-192}"
export MODEL_MAX_LENGTH="${MODEL_MAX_LENGTH:-2048}"
export DIFFUSION_NORM_STATS_PATH="${DIFFUSION_NORM_STATS_PATH:-/home/jovyan/data/siglip2_bn_stats.pt}"

echo "===== AURORA Stage 1: RefCOCO Multi-Slot Recon ====="
echo "Checkpoint source: ${MODEL_PATH}"
echo "Output dir:        ${OUTPUT_DIR}"
echo "Resume:            ${RESUME_FROM_CHECKPOINT:-<disabled>}"
echo "GPUs:              ${NUM_GPUS}"
echo "Per-device batch:  ${PER_DEVICE_TRAIN_BATCH_SIZE}"
echo "Grad accum:        ${GRADIENT_ACCUMULATION_STEPS}"
echo "BF16:              ${BF16}"
echo "TF32:              ${TF32}"
echo "Max slots:         ${CAPTIONSLOT_MAX_SLOTS}"
echo "Slots/object:      ${CAPTIONSLOT_SLOTS_PER_OBJECT}"
echo "Registers:         ${CAPTIONSLOT_N_REGISTER}"
echo "Vision towers:     ${VISION_TOWER_AUX_LIST}"
echo "Vision tokens:     image=${IMAGE_FEATURE_TOKEN_LEN} target=${DIFFUSION_TARGET_TOKEN_LEN}"
echo "Datasets:          ${CAPTIONSLOT_DATASETS}"
echo "Train/Eval splits: ${CAPTIONSLOT_TRAIN_SPLITS} / ${CAPTIONSLOT_EVAL_SPLITS}"
echo "Prior bias scale:  ${CAPTIONSLOT_PRIOR_BIAS_SCALE} (legacy no-op)"
echo "Cross-attn:        ${CAPTIONSLOT_ADD_CROSS_ATTENTION}"
echo "Recon loss wt:     ${CAPTIONSLOT_RECON_LOSS_WEIGHT}"
echo "Mask BCE wt:       ${CAPTIONSLOT_MASK_BCE_LOSS_WEIGHT}"
echo "Mask Dice wt:      ${CAPTIONSLOT_MASK_DICE_LOSS_WEIGHT}"
echo "Scheduler:         cosine-min=${CAPTIONSLOT_USE_COSINE_MIN_LR_SCHEDULE}, min_ratio=${CAPTIONSLOT_MIN_LR_RATIO}, warmup=${WARMUP_RATIO}"
echo "COCO root:         ${CAPTIONSLOT_COCO_ROOT}"
echo "======================================================="

exec bash "${SCRIPT_DIR}/train_aurora.sh"
