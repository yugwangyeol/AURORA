#!/bin/bash
# =============================================================================
# CaptionSlot Stage 1 wrapper
# =============================================================================

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export MODEL_PATH="${MODEL_PATH:-/home/jovyan/data/Scale-RAE-Qwen1.5B_DiT2.4B}"
export OUTPUT_DIR="${OUTPUT_DIR:-/home/jovyan/AURORA/checkpoints/captionslot_stage1}"
export EXPERIMENT_NAME="${EXPERIMENT_NAME:-captionslot_stage1}"

export NUM_GPUS="${NUM_GPUS:-2}"
export MAX_STEPS="${MAX_STEPS:-20000}"
export NUM_TRAIN_EPOCHS="${NUM_TRAIN_EPOCHS:-1}"
export PER_DEVICE_TRAIN_BATCH_SIZE="${PER_DEVICE_TRAIN_BATCH_SIZE:-16}"
export PER_DEVICE_EVAL_BATCH_SIZE="${PER_DEVICE_EVAL_BATCH_SIZE:-4}"
export GRADIENT_ACCUMULATION_STEPS="${GRADIENT_ACCUMULATION_STEPS:-2}"
export LOGGING_STEPS="${LOGGING_STEPS:-10}"
export EVAL_STEPS="${EVAL_STEPS:-1000}"
export SAVE_STEPS="${SAVE_STEPS:-1000}"
export DATALOADER_NUM_WORKERS="${DATALOADER_NUM_WORKERS:-8}"
export REPORT_TO="${REPORT_TO:-wandb}"
export BF16="${BF16:-False}"
export TF32="${TF32:-True}"
export LEARNING_RATE="${LEARNING_RATE:-5e-5}"
export DIFF_HEAD_LR="${DIFF_HEAD_LR:-5e-6}"
export CAPTIONSLOT_LATENT_QUERY_LR="${CAPTIONSLOT_LATENT_QUERY_LR:-1e-6}"

export CAPTIONSLOT_TRAINING_STAGE=1
export CAPTIONSLOT_MAX_SLOTS="${CAPTIONSLOT_MAX_SLOTS:-10}"
export CAPTIONSLOT_N_REGISTER="${CAPTIONSLOT_N_REGISTER:-8}"
export CAPTIONSLOT_CMD_LENGTH="${CAPTIONSLOT_CMD_LENGTH:-8}"
export CAPTIONSLOT_RECON_LOSS_WEIGHT="${CAPTIONSLOT_RECON_LOSS_WEIGHT:-1e-4}"
export CAPTIONSLOT_CAPTION_LOSS_WEIGHT="${CAPTIONSLOT_CAPTION_LOSS_WEIGHT:-1.0}"
export CAPTIONSLOT_DIVERSITY_LOSS_WEIGHT="${CAPTIONSLOT_DIVERSITY_LOSS_WEIGHT:-1.0}"
export CAPTIONSLOT_TRAIN_LATENT_QUERIES="${CAPTIONSLOT_TRAIN_LATENT_QUERIES:-False}"
export CAPTIONSLOT_CONDITION_GATE_INIT="${CAPTIONSLOT_CONDITION_GATE_INIT:-0.1}"
export CAPTIONSLOT_ATTENTION_USE_LAYERNORM="${CAPTIONSLOT_ATTENTION_USE_LAYERNORM:-True}"
export CAPTIONSLOT_ATTENTION_TEMPERATURE="${CAPTIONSLOT_ATTENTION_TEMPERATURE:-1.0}"
export CAPTIONSLOT_ADD_CROSS_ATTENTION="${CAPTIONSLOT_ADD_CROSS_ATTENTION:-True}"
export CAPTIONSLOT_CROSS_ATTENTION_START_BLOCK="${CAPTIONSLOT_CROSS_ATTENTION_START_BLOCK:-8}"
export CAPTIONSLOT_CROSS_ATTENTION_EVERY_N_BLOCKS="${CAPTIONSLOT_CROSS_ATTENTION_EVERY_N_BLOCKS:-4}"
export CAPTIONSLOT_CROSS_ATTENTION_INCLUDE_REGISTERS="${CAPTIONSLOT_CROSS_ATTENTION_INCLUDE_REGISTERS:-True}"
export CAPTIONSLOT_EVAL_NUM_IMAGES="${CAPTIONSLOT_EVAL_NUM_IMAGES:-64}"

export CAPTIONSLOT_IMAGE_FOLDER="${CAPTIONSLOT_IMAGE_FOLDER:-/home/jovyan/data/coco/train2017}"
export COCO_CAPTION_JSON="${COCO_CAPTION_JSON:-/home/jovyan/data/coco/annotations/captions_train2017.json}"
export CAPTIONSLOT_ANNOTATION_PATH="${CAPTIONSLOT_ANNOTATION_PATH:-/home/jovyan/data/coco/captions_with_chunks_train2017.json}"
export MAX_CAPTION_TOKENS="${MAX_CAPTION_TOKENS:-64}"
export DIFFUSION_NORM_STATS_PATH="${DIFFUSION_NORM_STATS_PATH:-/home/jovyan/data/siglip2_bn_stats.pt}"

echo "===== CaptionSlot Stage 1 ====="
echo "Checkpoint source: ${MODEL_PATH}"
echo "Output dir:        ${OUTPUT_DIR}"
echo "GPUs:              ${NUM_GPUS}"
echo "Per-device batch:  ${PER_DEVICE_TRAIN_BATCH_SIZE}"
echo "Grad accum:        ${GRADIENT_ACCUMULATION_STEPS}"
echo "Effective batch:   $((NUM_GPUS * PER_DEVICE_TRAIN_BATCH_SIZE * GRADIENT_ACCUMULATION_STEPS))"
echo "Learning rate:     ${LEARNING_RATE}"
echo "Diff head lr:      ${DIFF_HEAD_LR}"
echo "Latent query lr:   ${CAPTIONSLOT_LATENT_QUERY_LR}"
echo "BF16:              ${BF16}"
echo "TF32:              ${TF32}"
echo "Recon loss wt:     ${CAPTIONSLOT_RECON_LOSS_WEIGHT}"
echo "Caption loss wt:   ${CAPTIONSLOT_CAPTION_LOSS_WEIGHT}"
echo "Diversity wt:      ${CAPTIONSLOT_DIVERSITY_LOSS_WEIGHT}"
echo "Latent query train:${CAPTIONSLOT_TRAIN_LATENT_QUERIES}"
echo "Gate init:         ${CAPTIONSLOT_CONDITION_GATE_INIT}"
echo "Sparse xattn:      ${CAPTIONSLOT_ADD_CROSS_ATTENTION} (start=${CAPTIONSLOT_CROSS_ATTENTION_START_BLOCK}, every=${CAPTIONSLOT_CROSS_ATTENTION_EVERY_N_BLOCKS}, regs=${CAPTIONSLOT_CROSS_ATTENTION_INCLUDE_REGISTERS})"
echo "Train images:      ${CAPTIONSLOT_IMAGE_FOLDER}"
echo "Caption source:    ${COCO_CAPTION_JSON}"
echo "Chunk manifest:    ${CAPTIONSLOT_ANNOTATION_PATH}"
echo "==============================="

exec bash "${SCRIPT_DIR}/train_aurora.sh"
